[project]
name = "gpt-sovits"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ffmpeg>=1.4",
    "gradio>=5.23.1",
    "psutil>=7.1.3",
    "pyyaml>=6.0.3",
    "torch>=2.9.1",
]

[dependency-groups]
dev = [
    "basedpyright>=1.34.0",
    "ruff>=0.14.5",
]

[tool.uv]
override-dependencies = ["pydantic-core>=2.41"]